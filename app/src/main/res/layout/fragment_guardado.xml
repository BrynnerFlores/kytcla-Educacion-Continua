<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/constraint_layout_fragment_chat"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".view.guardado.FragmentGuardado">

    <com.google.android.material.tabs.TabLayout
        android:id="@+id/tab_layout_elementos_guardados"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="@drawable/tab_layout_bg"
        android:contentDescription="@string/app_name"
        app:layout_constraintBottom_toTopOf="@+id/view_pager_two_elementos_guardados"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:tabGravity="center"
        app:tabIndicator="@drawable/tab_indicator"
        app:tabIndicatorGravity="bottom"
        app:tabMode="auto"
        app:tabRippleColor="@android:color/transparent"
        app:tabTextAppearance="@style/AppTheme.Tabs.TextAppearance" />

    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/view_pager_two_elementos_guardados"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/tab_layout_elementos_guardados" />
</androidx.constraintlayout.widget.ConstraintLayout>